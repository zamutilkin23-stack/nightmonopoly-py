services:
  - type: web
    name: nightmonopoly-py
    runtime: python
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python wsgi.py
    disk:
      name: nightmonopoly-data
      mountPath: /opt/render/project/src/data